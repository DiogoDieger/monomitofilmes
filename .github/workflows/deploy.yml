name: Deploy to KingHost via FTP

on:
  push:
    branches:
      - main # Roda o workflow quando há push na branch 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4 # 🔥 Atualizado para versão correta
        with:
          server: "ftp.monomitofilmes.com.br"
          username: "monomitofilmes"
          password: "Huehuebr12"
          local-dir: "./out/"
          server-dir: "/home/monomitofilmes/www/"
          exclude: |
            **/.git*
            **/.DS_Store
            **/node_modules
            **/package-lock.json
          dangerous-clean-slate: true # ⚠ Apaga tudo na pasta www antes do upload
