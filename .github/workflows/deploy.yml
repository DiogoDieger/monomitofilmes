name: Deploy to KingHost via FTP

on:
  push:
    branches:
      - main # O workflow será executado em pushs na branch 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20" # Certifique-se de usar a versão do Node.js compatível com seu projeto

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build # Certifique-se de que este script gera a pasta 'out/'

      # 🔹 Força exclusão de todos os arquivos antes do deploy
      - name: Force Clean Remote Directory
        run: |
          sudo apt-get install -y lftp
          lftp -e "set ssl:verify-certificate no; chmod -R 777 /home/monomitofilmes/www/; mrm -rf /home/monomitofilmes/www/*; bye" -u monomitofilmes,"Huehuebr12" ftp.monomitofilmes.com.br

      # 🔹 Lista os arquivos na pasta /www depois da exclusão
      - name: List Files After Deletion
        run: |
          lftp -e "set ssl:verify-certificate no; ls /home/monomitofilmes/www/; bye" -u monomitofilmes,"Huehuebr12" ftp.monomitofilmes.com.br

      # 🚀 Faz upload dos arquivos novos via FTP
      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: "ftp.monomitofilmes.com.br"
          username: "monomitofilmes"
          password: Huehuebr12 # Use Secrets para segurança
          local-dir: "./out/"
          server-dir: "/home/monomitofilmes/www/"
          exclude: |
            **/.git*
            **/.DS_Store
            **/node_modules
            **/package-lock.json
          dangerous-clean-slate: true # Limpa a pasta de destino antes do upload

      # 🔹 Lista os arquivos após o deploy para confirmar que tudo subiu corretamente
      - name: List Files After Deploy
        run: |
          lftp -e "set ssl:verify-certificate no; ls /home/monomitofilmes/www/; bye" -u monomitofilmes,"Huehuebr12" ftp.monomitofilmes.com.br
